python -m uvicorn main:app --reload
docker ps
docker ps -a
docker images
docker start backend
docker start database
docker exec -it postgres_container  psql -U postgres -d my_database
docker exec -it backend bash
docker compose logs fastapi
docker compose logs --tail 100 fastapi
docker-compose build --no-cache
docker-compose up -d --force-recreate
SELECT * FROM members;
docker-compose down
docker-compose up -d --build
http://127.0.0.1:8000 
curl http://localhost:8000

지금 만들어진 이 백엔드를 배포하기 위해서 render.com 도메인을 만들었어. https://haueull-chun-fast.onrender.com/docs 를 보면 사진처럼 연동이 되어 있어. 그런데 customer/list도 조회가 안되고,  프론트엔드에서 로그인 요청을 https://haueull-chun-fast.onrender.com/api/account/user/login으로 보내고 있는데 '서버에 연결할 수 없습니다.'라는 문제가 발생하고, 서버 로그에 로그인 요청도뜨질않아. 기존에 http://localhost:8000/api/account/user/login으로 보냈을 때는 문제없이 작동했는데, 왜 이런 문제가 발생하는거니?